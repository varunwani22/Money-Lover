<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.moneylover">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SET_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_logo_monney_lover"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_logo_monney_lover"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoneyLover">
        <activity android:name=".views.EditTransactionActivity"></activity>
        <activity android:name=".views.ShowTransactionDetails" />
        <activity android:name=".views.WebViewActivity" />
        <activity
            android:name=".views.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".views.MainActivity" />
        <activity android:name=".views.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.login_page.LoginActivity" />
        <activity android:name=".views.RegisterActivity" />

        <activity android:name=".views.SignInActivity" />
    </application>

</manifest>